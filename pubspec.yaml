name: egyption_foods
description: "Egyption Foods Project."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  get: ^4.7.2
  restart_app: ^1.3.2
  firebase_auth: ^5.5.2
  fluttertoast: ^8.2.12
  firebase_core: ^3.13.0
  cloud_firestore: ^5.6.6
  cupertino_icons: ^1.0.8
  carousel_slider: ^5.0.0
  flutter_launcher_icons: ^0.14.3
  loading_animation_widget: ^1.3.0
  flutter_staggered_grid_view: ^0.7.0
  http: ^1.3.0
  google_maps_flutter: ^2.12.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter_icons:
  ios: true
  android: true
  adaptive_icon_background: "#FFFFFF"
  image_path: "assets/images/logo.png"

flutter:
  uses-material-design: true
  assets:
    - assets/
    - assets/images/